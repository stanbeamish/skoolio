import 'package:flutter/material.dart';

const kMidBoldLetters = TextStyle(
  color: Colors.black,
  fontSize: 25,
  fontWeight: FontWeight.w600,
  shadows: [
    BoxShadow(
      offset: Offset(0, 0),
      color: Colors.white,
      blurRadius: 10.0,
      spreadRadius: 10.0,
    )
  ],
);

const kSmallBoldLetters = TextStyle(
  color: Colors.black,
  fontSize: 15,
  fontWeight: FontWeight.w600,
  shadows: [
    BoxShadow(
      offset: Offset(0, 0),
      color: Colors.white,
      blurRadius: 10.0,
      spreadRadius: 10.0,
    )
  ],
);
