import 'package:flutter/material.dart';

const kBoldLetters25 = TextStyle(
  color: Colors.white,
  fontSize: 25,
  fontWeight: FontWeight.w600,
  shadows: [
    BoxShadow(
      offset: Offset(0, 0),
      color: Colors.black,
      blurRadius: 10.0,
      spreadRadius: 10.0,
    )
  ],
);

const kNormalLetters25 = TextStyle(
  color: Colors.white,
  fontSize: 25,
  fontWeight: FontWeight.w200,
  shadows: [
    BoxShadow(
      offset: Offset(0, 0),
      color: Colors.black,
      blurRadius: 10.0,
      spreadRadius: 10.0,
    )
  ],
);

const kBoldLetters20 = TextStyle(
  color: Colors.white,
  fontSize: 20,
  fontWeight: FontWeight.w600,
  shadows: [
    BoxShadow(
      offset: Offset(0, 0),
      color: Colors.black,
      blurRadius: 10.0,
      spreadRadius: 10.0,
    )
  ],
);

const kNormalLetters20 = TextStyle(
  color: Colors.white,
  fontSize: 20,
  fontWeight: FontWeight.w300,
  shadows: [
    BoxShadow(
      offset: Offset(0, 0),
      color: Colors.black,
      blurRadius: 10.0,
      spreadRadius: 10.0,
    )
  ],
);

const kBoldLetters15 = TextStyle(
  color: Colors.white,
  fontSize: 15,
  fontWeight: FontWeight.w600,
  shadows: [
    BoxShadow(
      offset: Offset(0, 0),
      color: Colors.black,
      blurRadius: 10.0,
      spreadRadius: 10.0,
    )
  ],
);


const kNormalLetters15 = TextStyle(
  color: Colors.white,
  fontSize: 15,
  fontWeight: FontWeight.w300,
  shadows: [
    BoxShadow(
      offset: Offset(0, 0),
      color: Colors.black,
      blurRadius: 10.0,
      spreadRadius: 10.0,
    )
  ],
);
